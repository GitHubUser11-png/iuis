name: Disabled Pass 11 Test Contract Helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This temporary helper is removed by the corrective reconciliation workflow."
